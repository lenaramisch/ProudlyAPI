type: object
properties:
  user_id:
    type: number
    format: int64
    example: 2
  title:
    type: string
    example: Take a shower
  size:
    type: string
    enum: [small, medium, big]
    example: small
required:
  - title
  - size
